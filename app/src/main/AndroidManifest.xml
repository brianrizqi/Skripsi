<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.onestep.skripsi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <meta-data
        android:name="android.max_aspect"
        android:value="2.1" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.EditProfile"></activity>
        <activity android:name=".Activities.Hasil" />
        <activity android:name=".Activities.TambahTanamanSPPK" />
        <activity android:name=".Activities.TambahTanamanManual" />
        <activity android:name=".Activities.Tanaman" />
        <activity android:name=".Activities.TambahLahan" />
        <activity android:name=".Activities.DetailArticle" />
        <activity android:name=".Activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Masuk" />
        <activity android:name=".Activities.Daftar" />
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>